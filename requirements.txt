# ParkPulse Python dependencies (Python 3.10+)

# --- Core data ---
pandas
numpy
pyarrow            # read/write the cleaned parquet
scipy
scikit-learn

# --- Spatial (H3 v4 API: h3.latlng_to_cell / cell_to_boundary / cells_to_geo) ---
h3>=4.0

# --- Plotting (EDA figures + shared scripts/style.py palette) ---
matplotlib
seaborn

# --- Interactive map (impact-weighted hotspot map, Step 2/3) ---
folium

# --- Forecaster (Step 7): gradient-boosting bake-off ---
lightgbm           # Tweedie objective
xgboost            # Tweedie objective
catboost           # Tweedie objective
joblib             # persist the trained model

# Optional / run-locally:
#   statsmodels         # NB/ZINB statistical alternative
#   osmnx>=2.0          # real road class + POIs (scripts/enrich_osm.py; needs network)
#   python-docx         # render the submission docs to .docx (submission/make_docx.py)
