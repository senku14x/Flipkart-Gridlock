# --- Raw dataset ---
# violations.csv is ~105MB and exceeds GitHub's 100MB limit (see CLAUDE.md §10).
# Matched in any directory so it can never be committed by accident.
violations.csv

# --- Python ---
__pycache__/
*.pyc
*.pyo
.ipynb_checkpoints/

# --- Virtual environments ---
.venv/
venv/
env/

# --- Training cruft ---
catboost_info/

# --- Submission packaging (regenerate with: git archive --format=zip -o submission/ParkPulse_source.zip HEAD) ---
submission/ParkPulse_source.zip

# --- Local / OS ---
.env
.DS_Store
